// sempre escrever como se fosse uma pergunta

const isUserAvailable = true;

// is, does, has

// errado
const disabled = true;
const isDisabled = true;

const userHasGroup = true;

// ---------

// se está disabilitado, ai sim faz algo, traz uma legitibilidade maior

if (isDisabled) {
  console.log(isDisabled);
}

//  Boolean sempre como perguntar / maximo semântico possivel

const loading = false;

//  junção de pergunta com nomes declarativos do que faz

const isUserProfileLoading = true;

// leia um booleano sempre como um if na frente, se continuar com as regras da gramatica está no contexto certo
