// numeros magicos sao numeros que aparecem no codigo, como uma data transformando em segundos, ou segundos transformados em data

//simples

const INTERVAL_30_DAYS = 1000 * 60 * 60 * 24 * 30;

setTimeout(() => {}, INTERVAL_30_DAYS);

// Avançado

// quando vai lidar em preços normalmente salva em centavos
// quando for lidar com hora salva em minutos ao inves de horas

function calculateDiscount(priceInCents, dicountAmountInPercentage) {
  // return desconto
}

// ! COMENTÁRIO

// são como avisos dentro do codigo, comentario é normal ter no codigo
// documentacao nao deve ficar dentro do comentario
