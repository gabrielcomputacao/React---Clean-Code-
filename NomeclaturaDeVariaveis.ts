// evitar diminutivos, exemplo : U, D, pot

// não e sobre variavel pequena, e sim dizer exatamente o que ele faz

// Evitar nomes genericos

const users = ["gabriel", "tf", "morgana"];

const usersStartingWithLetterG = users.filter((user) => user.startsWith("g"));
